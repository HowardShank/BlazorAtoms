<Project Sdk="Microsoft.NET.Sdk.Razor">

  <Import Project="$(RepoRoot)build\Packable.props" />
  <Import Project="$(RepoRoot)build\Shared.props" />

  <PropertyGroup>
    <PackageId>BlazorAtoms.BusyIndicators</PackageId>
    <Title>Blazor Busy Indicators</Title>
    <Description>Self-contained animated SVG busy/activity indicators for Blazor (Server or WebAssembly). No JS, no dependencies.</Description>
    <PackageTags>Blazor;BusyIndicator;ActivityIndicator;Busy;Spinner;SVG;Loading;WebAssembly;Server</PackageTags>
    <Version>0.1.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <SupportedPlatform Include="browser" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.Web" />
  </ItemGroup>

  <ItemGroup>
    <TrimmerRootDescriptor Include="ILLink.Descriptors.xml" />
  </ItemGroup>

</Project>
