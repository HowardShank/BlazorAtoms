namespace BlazorAtoms.Progress;

/// <summary>Which fixed edge of the viewport <see cref="AtomScrollProgressBar"/> sticks to.</summary>
public enum ScrollProgressPosition
{
    /// <summary>Fixed to the top of the viewport.</summary>
    Top,

    /// <summary>Fixed to the bottom of the viewport.</summary>
    Bottom,
}
