namespace BlazorAtoms.Equipment;

/// <summary>Housing layout for an <see cref="AtomBattery"/> — the classic landscape shape with the
/// terminal nub on the right (<see cref="Horizontal"/>), or turned upright with the nub on top
/// (<see cref="Vertical"/>).</summary>
public enum BatteryOrientation
{
    Horizontal,
    Vertical,
}
