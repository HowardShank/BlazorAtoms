# BlazorAtoms — Library Catalog & Roadmap

A living planning doc for how the BlazorAtoms family of component libraries is organized.
Each library ships as its own NuGet package, standalone, with ~0 third-party dependencies,
usable in any Blazor render mode.

> **Design philosophy — lightweight, drop-in, no lock-in.**
> Every BlazorAtoms library is a small, self-contained "atom": add just the one you need and it
> works. No shared runtime dependency, no umbrella package, no design-system framework to buy
> into, no global setup/theme provider, no `builder.Services.Add…()` registration, and no
> JS bundle to wire up. Each component is plain Blazor + CSS/SVG that reads its inputs from
> `[Parameter]`s and styles from CSS variables, so it **intermixes freely with any application** —
> a greenfield app, a legacy page, or alongside a heavy component suite (MudBlazor, Radzen,
> Fluent, Telerik) without conflict. Take one component, take one library, or take several; you
> never inherit a large framework just to use a spinner.
>
> **One deliberate exception:** `BlazorAtoms.Transitions` carries a real `ProjectReference` to
> `BlazorAtoms.Behaviors`, for the runtime CSS-capability check its `@starting-style`/JS-fallback
> hybrid needs. This is a genuine shared *capability* package (not a grab-bag "Core"), scoped and
> acknowledged rather than silently breaking the 0-deps rule — every other library still stands
> alone.

## Naming convention

- **Library** = package id = assembly = root namespace = `BlazorAtoms.<Area>`.
  - `<Area>` is PascalCase: **plural** for countable families (`Inputs`, `Cards`, `Charts`),
    a mass noun otherwise (`Layout`, `Progress`, `Navigation`).
- **Public component** = `Atom<Name>` for both the type and the tag, e.g. `AtomActivityGears` → `<AtomActivityGears />`.
- **Component groups** within a library = subfolder + matching sub-namespace (e.g. `Indicators/`).
- **Dependencies**: framework only (`Microsoft.AspNetCore.Components.Web`); no third-party runtime packages.
  Shared code lives in `BlazorAtoms.Shared` and is compiled in via `build/Shared.props` (no package dependency).

Libraries are tiered below by how well they fit the ethos: standalone, ~0-dependency,
self-contained SVG/CSS, JavaScript-free.

## JavaScript policy

JavaScript is allowed only where a browser primitive genuinely requires it (focus-trap,
scroll-lock, smart positioning, clipboard, Resize/IntersectionObserver) — and it must be
**invisible to the consumer**:

- The library ships its own small JS module as a static web asset
  (`wwwroot/atom-<area>.js` → `_content/BlazorAtoms.<Area>/atom-<area>.js`) and **lazy-imports it
  itself** via an `IJSObjectReference` on first use. No `<script>` tag, no DI registration, no setup.
- Each JS-using library carries its own module — no shared JS package (that would be a dependency).
- Logic and state stay in C#; JS does only the primitive the platform can't do declaratively.
- Prefer native features first: `<details>/<summary>`, the HTML **Popover API**, CSS **anchor
  positioning**, and `<dialog>` remove most classic JS needs.
- Caveat: JS interop can't run during static SSR / prerender — JS components render markup first and
  enhance once interactive. JS-free libraries behave identically in every render mode.

## Graphics policy

Most every graphic the components draw is **inline SVG** — vector, crisp at any zoom/DPI, themeable via
CSS custom properties and `currentColor`, animatable in CSS. This covers charts, gauges, sparklines,
rating icons, QR/barcodes, skeleton shapes, and icons.

- **No/Limited raster for our own chrome** — want crisp clear SVG, not PNG/JPG UI assets, whenever possible.
- **Avoid `<canvas>`** (it is raster *and* needs JS); use SVG unless there is a hard performance reason.
- The only raster we own is the 128×128 NuGet package icon (packaging metadata, never shipped into
  apps). **User content** (an avatar photo, an image inside a card) is raster by nature — we render
  it inside SVG/CSS chrome but don't control its quality; our frame is always vector.

---

## Tier A — strong fit (pure SVG/CSS, JS-free). Build these first.

| Library | Components (`Atom*`) |
|---|---|
| `BlazorAtoms.ActivityIndicators` *(shipped)* | AtomActivityGears, AtomActivityDna, AtomActivityFunnel, AtomActivityHourglass, AtomActivityMagnifier, AtomActivityNeural, AtomActivitySwarm, AtomPulseBar, AtomPulseScanner |
| `BlazorAtoms.Progress` *(shipped)* | **The four determinate indicators, all shipped** — **AtomProgressBar** (horizontal, optional buffered band, readout inside/outside/above), **AtomProgressRing** (SVG arc; `pathLength="100"` re-bases the dash math onto a 0–100 scale so the arc has no π in it and is radius-independent — the stroke width resolves in C# rather than CSS because `r` depends on it and SVG2 geometry properties aren't portable), **AtomProgressSteps** (discrete wizard tracker; `Current` index, connectors as each item's own `::before`, markers become real `<button>`s only when `OnStepClick` is wired), **AtomMeter** (`role="meter"` — a measurement, not a task; reimplements the HTML `<meter>` spec's three-way low/high/optimum classification as `data-level`, because the native element's bar is drawn by vendor pseudo-elements that can't be themed consistently). Two bases: **AtomProgressBase** (label/readout, `Variant`/`Size`/`Effect` axes as `data-*` over a `--progress-*` surface) and **AtomProgressValueBase** (adds `Value`/`Min`/`Max`) — split so `AtomProgressSteps`, which has no continuous value, doesn't inherit `Min`/`Max` as dead params. **`Value` is `double?` and null means indeterminate** — one param instead of a value plus a flag, so "`Value=40` but ignore it" is unrepresentable; `aria-valuenow` is omitted, which is how ARIA spells it. All four are JS-free. AtomScrollProgressBar *(shipped)* — fixed reading-progress bar tracking page scroll position; shares neither base (it has no `Value`); CSS scroll-driven animation (`animation-timeline: scroll()`) natively, Chromium-only, with a small self-contained JS fallback elsewhere (no `BlazorAtoms.Behaviors` dependency — inline capability check instead, to avoid growing that "one exception" beyond `BlazorAtoms.Transitions`) |
| `BlazorAtoms.Skeletons` *(shipped)* | **One painted primitive plus three presets.** **AtomSkeletonBlock** is the only component here whose stylesheet paints — `Width`/`Height`/`Radius` over the `--skeleton-*` surface. **AtomSkeletonText** (a flex column of blocks; `Lines`, `LastLineWidth` for the ragged final line, deterministic widths so prerender and interactive agree), **AtomSkeletonAvatar** (exactly one block, no wrapper and no stylesheet; `Size` + `Shape`, and deliberately **no `Radius`** since `Shape` owns the corners and `Circle` would silently ignore one) and **AtomSkeletonCard** (media band + avatar + text group) all render blocks and contribute layout only, so the shimmer gradient, the pulse keyframe and the `prefers-reduced-motion` fallback exist once instead of four times — the same call `BlazorAtoms.Buttons` makes with `AtomIconButton`. `Animation` is Shimmer/Pulse/None, always-on (a skeleton has no trigger and no state). **Accessibility is opt-in:** the root is `aria-hidden` by default, becoming `role="status" aria-live="polite"` only when given an `AriaLabel`, and the label is not forwarded to composed children — one card is one announcement, not five. A consequence of composing rather than restyling: presets pass *parameters* to their blocks, never classes, because the scope rewriter stamps a child's own scope id and a cross-component rule would silently never match. JS-free in every render mode |
| `BlazorAtoms.Charts` *(shipped)* | **AtomSparkline** (bare trend line, no chrome), **AtomLineChart**, **AtomBarChart** (`Orientation`, `BarGap`, `Radius`), **AtomDonut** (shares of a total, `Palette`) and the **gauge family** — **AtomGauge** (dial with `Bands`, needle, `SweepAngle`), **AtomBarGauge** (straight track, `Orientation` × `BarStyle`: Segmented/Gradient/Ticks, triangular pointer) and **AtomDotGauge** (a row of dots growing from a small dim start color to a large full-hue end color, active dot marked). One series of `double`s plus optional positional `Labels` — no generics, no library types for the data. **Every piece of chrome is an opt-in child component in a named slot**, each with its own `CssClass`/`Style` and stylesheet: `AtomChartHeading`/`Caption`/`Legend`/`EmptyState` on all charts, `ValueAxis`/`CategoryAxis`/`AxisTitle`/`ValueLabels`/`Gridlines`/`Baseline` on the cartesian pair, `Center`/`SliceLabels` on the donut, `Readout`/`RangeLabels` shared by all three gauge-family members. A chart with no slots filled draws only its marks. **Named slots, not flat children, and that is forced:** Blazor initializes a child *after* the parent's render pass, so a chart could never read a child's parameters while reserving the value-axis gutter — and the package promises single-pass parity under static SSR. A slot is a parameter, so `ValueAxis is not null` is readable in time. Hence the governing rule — *on the chart if the chart must know it before it renders* (`ValueAxisWidth`, `LegendPlacement`, `GridlineCount`/`NiceScale`), on the element otherwise. Elements are pure presentation: the chart precomputes every coordinate into a `ChartContext` cascaded `IsFixed="false"` and rebuilt per render (the `CardContext` pattern — read-only children need no registration list and no second pass, unlike `AtomTabs`). Scoped CSS can't cross into a slot, so each element owns its stylesheet, cross-boundary values travel as `--chart-*` properties, and `AtomBarChart` uses `::deep` for the layout it owns on markup it doesn't. **`AtomGaugeBase`** (new) is a fifth base under `AtomChartBase`, shared by all three gauge-family members: `Value`/`Min`/`Max`/`Formatter`, `SegmentCount`/`StartColor`/`EndColor` (auto-generates N `GaugeBand`s via `GaugeColorScale`'s HSL **hue-sweep** interpolation — red→green swept through hue space rather than RGB-lerped, so the midpoint lands on orange/yellow instead of a muddy olive — `Bands` set explicitly always wins), `Elevation` (`Flat`/`Raised`/`Floating`, **defaults to `Floating`** — a two-tier `drop-shadow` plus a blurred sheen shape, filter-based only, never a 3D/perspective transform, the same technique `AtomStoplight`'s glow uses), `SegmentLabels` (one label per evenly-spaced segment in the `RangeLabels` slot instead of just the two endpoints — e.g. `["Poor","Fair","Good","Excellent"]`), and `Banner` (a dark pill straddling the shape's own bottom edge, half on/half off — distinct from `Caption`, which the frame places below everything instead; put an `AtomChartBanner` here). `AtomGauge` also has `NeedleStyle` (`Line`/`Tapered`/`Triangle`/`RimTab` — `Tapered` is a dart with a tail counterweight and a two-layer hub, `Triangle` a short bold arrowhead off the face plate with no tail, `RimTab` isn't a centre-pivot needle at all but a small tab riding the band's own rim at the value position — none of these a CSS override on a plain `<line>` could fake), a fixed light-neutral face plate (deliberately **not** theme-adaptive — a browser in dark mode was making an earlier `prefers-color-scheme` version of this render near-black) so the dial reads as an instrument face, a bezel ring (`BezelColor`/`BezelWidth`, default a hairline) framing it, and `ShowPedestal` (a literal drawn platform under the dial, distinct from `Elevation`'s drop-shadow). `AtomChartReadout`'s default look is a solid dark pill with white text — fixed, not a currentColor tint, so it reads the same regardless of the page's own theme. Geometry (arc trig, bar layout, dot spacing) stays private to each concrete gauge component, same split as `AtomGauge`/`AtomDonut` despite both drawing circles. **Five bases** in total: `AtomChartBase`, `AtomSeriesChartBase`, `AtomCartesianChartBase`, `AtomGaugeBase`, plus `AtomChartElementBase` — reflection tests pin which slots each chart refuses. **Bars are zero-based** (a bar's length *is* its value) while lines scale to their data. Degenerate series — empty, single-point, dead-flat, negative — are ordinary query results and are handled rather than thrown on, because bad geometry doesn't throw, it emits `NaN` and draws nothing. Arcs use `pathLength="100"` so dash maths is radius-independent, as in `AtomProgressRing`; hover values and accessible names are SVG `<title>` elements, so there is no positioning code and no interop. `role="img"` with a generated summary. Deliberately *not* overlapping `Progress`: a donut is many slices (not one fraction of a task) and a gauge is a graphic (not `role="meter"` — that's `AtomMeter`). JS-free in every render mode |
| `BlazorAtoms.Badges` *(shipped)* | AtomBadge — count/label badge in many shapes (pill/circle/square/rounded + SVG star/hexagon/diamond/shield/burst/ribbon); overlays a host corner or renders inline; any object value w/ type-aware formatting; motion opt-in via `Animation` (default `None`) with Pop/Bounce/Spin/Pulse/Ping (merged the old AtomStaticBadge + AtomAnimatedBadge) · AtomChip — interactive chip (icon slot, click/select, removable) · AtomTag — display categorization label (rounded rect, removable) · AtomPill — status pill (fully-rounded soft tint + leading dot). Chip family shares Variant + Solid/Soft/Outline Appearance. |
| `BlazorAtoms.Avatars` *(shipped)* | AtomAvatar — silhouette (solid/gradient) or image, cropped to circle/square/rounded/squircle/hexagon, bg color/gradient+angle, border ring · AtomInitialsAvatar — initials from name w/ deterministic palette color · AtomAvatarGroup — overlapping stack from names w/ "+N" overflow |
| `BlazorAtoms.Ratings` *(shipped)* | AtomRating — one component for both a read-only display (true fractional fill, e.g. 4.3/5) and an interactive input (hover preview, click, keyboard, snapping `Step`). Value is `double?` so `null` is a distinct "unrated" state; clearable; built-in star/heart/circle/square/diamond/triangle/thumb/bolt shapes or a custom SVG path; per-instance colors; optional value + count label |
| `BlazorAtoms.Layout` | **AtomDrawer** — overlay drawer panel with position (left/right/top/bottom), transitions (slide/fade/pop/bounce/grow), sizing, and declarative styling; future: container anchoring. AtomStack, AtomGrid, AtomDivider, AtomSpacer, AtomCenter, AtomAspectRatio (planned). |
| `BlazorAtoms.Transitions` *(shipped)* | **AtomTransition** — generic wrapper that plays a CSS enter/exit transition (`AtomTransitionEffect`: Fade, Pop, FadeScale, SlideUp/Down/Left/Right, ShiftBlur, FlipY20/FlipYNeg20/FlipX20/FlipXNeg20) around arbitrary child content on a `Show` toggle. CSS-native (`@starting-style`) where supported, JS fallback elsewhere — see JavaScript policy note below. **AtomHoverEffect** — generic wrapper, same "arbitrary `ChildContent`" shape, but hover-triggered: pure CSS `:hover`/`:active`, no C# state. `HoverEffect` enum picks the treatment — `Sparkle` (scattered SVG sparkles; positions are a deterministic function of index, not `Random`, to avoid a hydration-mismatch jump) and `Tilt` (decorative 3D lift, `TiltDegrees`/`TiltPerspective`). Both members share one parameter surface, which is what keeps them on an enum instead of becoming separate components — `Tilt` lives here rather than in `BlazorAtoms.Cards` because it reveals nothing and so needs no card structure; wrap any card in it to compose. **AtomHoverGlow** — wraps several children (any elements) and glows whichever is currently hovered/focused, sliding between them; pure CSS anchor positioning where supported (Chromium only today), JS fallback (event delegation + bounding-box tracking) elsewhere via `AtomBrowserSupport`. |
| `BlazorAtoms.Barcodes` *(implemented)* | AtomBarcode (1D), AtomQrCode (2D) — own C# encoder → SVG; generation only |
| `BlazorAtoms.Data` *(shipped)* | AtomDataHasher — live CRC-32 / CRC-64 / MD5 / SHA-256 / SHA-512 hex-digest panel over a text input; CRC engines implemented in-library (no `System.IO.Hashing` dependency), cryptographic engines wrap `System.Security.Cryptography`; algorithm picker toggle, `EditContext`-aware validation, `Multiline` textarea vs single-line input, `ResultColor`/`ResultBackgroundColor` theming; no JS |
| `BlazorAtoms.DragDrop` *(shipped)* | AtomDropzone&lt;TItem&gt; — generic drag-and-drop list using native HTML5 DnD (no JS), single-list reorder + cross-zone transfer with explicit group scoping via `<AtomDropzoneGroup>` and/or `Group=` key, `Accepts` / `AllowsDrag` / `CopyItem` / `MaxItems` / `InstantReplace` predicates, Vertical / Horizontal / Grid orientation, `Virtualize` support, scoped CSS with `--dropzone-highlight-color` / `--dropzone-deny-color` / `--dropzone-gap` custom properties, all events surfaced as `EventCallback<>`; pure `DropzoneEngine` reorder helper exposed for headless use |
| `BlazorAtoms.Scrollbars` *(shipped)* | AtomScrollbar — generic `ChildContent` wrapper giving its own scroll box a themed scrollbar (size, track/thumb color, optional 2-stop thumb gradient, radius, border, hover color); `Axis` (Vertical/Horizontal/Both) picks which `overflow-*` is active. Zero-JS: full control via `::-webkit-scrollbar` pseudo-elements on Chrome/Edge/Safari, `scrollbar-color`/`scrollbar-width` solid-color fallback on Firefox. |
| `BlazorAtoms.Breadcrumbs` *(shipped)* | AtomBreadcrumbBar — renders the trail as semantic `<nav><ol>` with the current page as `aria-current`, `Separator` and `LoadingPlaceholder` configurable · AtomBreadcrumbProvider — cascades the service, so there is no `builder.Services.Add…()` call · AtomBreadcrumbService + AtomBreadcrumbGraph + AtomBreadcrumbEntry — the trail model. **Two sources, one chain:** a `[AtomBreadcrumb]` attribute declares a page's title and parent route so the trail is rebuilt from the route alone and survives a refresh or a deep link (no navigation history needed), while `Push` covers pages that can't be declared statically. Lives outside `BlazorAtoms.Navigation` because it needs no JS at all, and folding it in would drag `atom-navigation.js` along with it. |
| `BlazorAtoms.Equipment` *(in progress)* | Semi-realistic status indicators for physical objects (a light, a signal, a motor, a door, a window, an elevator — anything with a discrete real-world state, not a task-progress %, which is `Progress`'s job). **AtomStoplight** *(shipped)* — traditional red/yellow/green traffic signal, `StoplightState` picks the lit lamp, `Orientation` (Vertical/Horizontal, always Red→Yellow→Green in reading order); one-way `State` (the host owns it, no auto-cycle, no `StateChanged`) since a real signal isn't something a viewer reaches out and flips. Each lamp is a rim/glass/sheen stack under a small visor (an annulus-segment `<path>` computed in C#, no `<defs>`), so there is no SVG gradient/id to keep unique across instances on one page — the active lamp instead switches `color` from a dim `color-mix()` tint to its full hue and a `currentColor` `drop-shadow` glow rides along with it, both off one property flip. Colors override per hue (`RedColor`/`YellowColor`/`GreenColor` → `--stoplight-red/-yellow/-green`). **AtomLightBulb** *(shipped)* — Edison-screw bulb (glass globe, zigzag filament + lead wires, threaded metal base as plain primitives, no path math needed), the same `color`-driven glow trick as the stoplight lamp. **AtomLightSwitch** *(shipped)* — wall plate + toggle lever, the lever an SVG `rotate(angle cx cy)` transform (the same technique `AtomAnalogClock` uses for its hands, not a CSS `transform-origin`, so it needs no extra browser support). Unlike the stoplight, **both bulb and switch are interactive**: click or Space/Enter flips `IsOn` and raises `IsOnChanged`, so `@@bind-IsOn` works directly — a light is something a viewer actually operates. `AtomLightSwitch` is deliberately distinct from `BlazorAtoms.Inputs`' `AtomSwitch`: that one is a generic pill-shaped form toggle, this one is a realistic physical wall-switch object. **AtomFan** *(shipped)* — also interactive: click or Space/Enter cycles `FanSpeed` (Off→Low→Medium→High→Off) and raises `SpeedChanged`, so `@@bind-Speed` works directly; `Direction` (Forward/Reverse) stays one-way and un-cycled, since a real reverse switch is separate hardware from the speed pull-chain. `Kind` (Desk/Ceiling) swaps the housing artwork — a grille + base stand for Desk, a bare hub for Ceiling — around the same shared blade/spin mechanics, rather than being two components. Blades spin via a CSS `animation` on the blade `<g>` (duration keyed to speed, `animation-direction` off `Direction`), off under `prefers-reduced-motion: reduce`. The direction arrow is one path, mirrored with `scaleX(-1)` for Reverse rather than a second computed path. Its speed readout is an HTML `<div>` sibling to the `<svg>`, not an SVG `<text>` — Razor can't author `<text>` (RZ1023), the same constraint documented for `BlazorAtoms.Charts`. **AtomBattery** *(shipped)* — outline shell + terminal nub with a fill `<rect>` sized to `Level` (`Empty`/`Quarter`/`Half`/`ThreeQuarter`/`Full`, red→amber→green by default), plus an independent `Status` badge (`None`/`Charging`/`Warning`/`Error`/`Slash`/`Unknown`/`Check`) layered on top — level and status are orthogonal, the same way a real battery icon composes a bolt over the fill glyph rather than replacing it. `Orientation` (Horizontal/Vertical) swaps the shell to upright with the nub on top; the fill always grows from the end farthest from the nub toward it, so "charging up" reads the same regardless of orientation. `Monochrome` collapses every level/status hue to the outline color for a flat black/white icon look; `OutlineColor`/`FillColor` override the defaults directly. One-way and presentational like the stoplight — a battery's charge is sensor data, not something a click should change. The `Unknown` badge's "?" is the one SVG `<text>` in the family, built via `RenderTreeBuilder` for the same RZ1023 reason as the fan's label. All five components are JS-free. *Planned:* a door/window open-closed monitor, an elevator direction indicator. |

On-brand standout: **`Charts`** — SVG, JS-free, same DNA as the activity indicators.

**What each does:**
- **ActivityIndicators** — animated "working…" loaders (gears, DNA, pulse bars) for when a task is running with no known progress %.
- **Progress** — shows how far along a task is: a filling bar, ring, or step tracker. Use when you *do* know the percentage (uploads, wizards); leave `Value` null and it turns indeterminate instead. `AtomMeter` is the odd one semantically — a measurement that just *is* what it is (disk used, a score, password strength) rather than a task advancing, hence `role="meter"` and the low/high/optimum bands. `AtomScrollProgressBar` is the other exception — determinate but driven by scroll position, not an explicit `Value`.
- **Skeletons** — grey placeholder shapes shown while data loads, roughly matching the layout of the content to come, usually with a shimmer (the grey boxes YouTube/LinkedIn show before content paints). Less jarring than a spinner. Swap in the real component when data arrives. Shipped as `AtomSkeletonBlock` (the rectangle you compose anything from) plus `AtomSkeletonText`, `AtomSkeletonAvatar` and `AtomSkeletonCard` presets; only the block paints, the presets render blocks. Decorative to assistive tech unless you name one with `AriaLabel`.
- **Charts** — small data visuals drawn as SVG (sparkline, bar, line, donut, gauge) — lightweight, no charting library. Shipped: hand one series of numbers to any of the five and get a crisp vector graphic, with the hover tooltip coming from an SVG `<title>` rather than from any JavaScript. Chrome is composed rather than configured: every axis, label, legend, readout and empty state is a child component you place in a named slot, so each one is independently styleable and none of it is drawn unless asked for. Single-series by design; a second chart element covers most comparisons, and multi-series can be added later without breaking the current API.
- **Badges** — tiny labels or counts attached to things: status pills, tags, the red "3" notification count on an icon.
- **Avatars** — circular/square images that stand in for a person or entity. `AtomInitialsAvatar` falls back to initials (or a placeholder) when there's no photo; `AtomAvatarGroup` stacks several with overlap and a "+N" overflow. Common in team lists, comment threads, and assignee rows.
- **Ratings** — star/heart scales in a single component. `AtomRating` shows a read-only score with true fractional fill (★★★★☆ 4.3/5) or, by default, is an interactive input (hover preview, click, keyboard) that snaps to a configurable `Step` (whole, half, or finer). The value is `double?` so `null` is a distinct "unrated" state (not a real 0); also clearable, a built-in icon shape or a custom SVG path, and an optional value/count label. *(Shipped as one `AtomRating` — the earlier two-component `AtomRatingInput` split was dropped in favor of a `ReadOnly` toggle.)*
- **Layout** — invisible structural building blocks: stack (spacing), grid, divider, spacer, centering — the scaffolding you arrange other components inside.
- **Transitions** — `AtomTransition` wraps arbitrary content and plays a reusable enter/leave animation (fade, slide, flip, ...) whenever `Show` toggles. CSS-native (`@starting-style`) on modern browsers with a JS fallback elsewhere, decided at runtime via `BlazorAtoms.Behaviors`'s capability check — see that library's entry and the JavaScript policy note below for the one deliberate dependency exception this creates.
- **DragDrop** — generic list drag-and-drop with `AtomDropzone<TItem>`. Native HTML5 DnD only (no JS), single-list reorder + cross-zone transfer via an outer `<AtomDropzoneGroup>` cascading context, optional `Group` key for finer scoping, `Accepts` / `AllowsDrag` / `CopyItem` / `MaxItems` / `InstantReplace` predicates, and Vertical / Horizontal / Grid orientation.
- **Scrollbars** — `AtomScrollbar` wraps any content in a scroll box with a custom-themed
  scrollbar (color, size, gradient, radius, border, hover) per instance, instead of the browser's
  default chrome. Full control on Chrome/Edge/Safari via `::-webkit-scrollbar`; Firefox falls back
  to solid colors via `scrollbar-color`/`scrollbar-width` (no gradient/radius/hover support there).
- **Breadcrumbs** — the "Home / Products / Widget" trail showing where you are and letting you climb
  back up. `AtomBreadcrumbBar` renders it; `AtomBreadcrumbProvider` cascades the service so nothing
  needs registering. The trail comes from a `[AtomBreadcrumb]` attribute on each page declaring its
  title and parent, so it is derived from the route rather than from navigation history — a refresh
  or a pasted deep link still shows the full chain. `Push` handles pages that can't declare it
  statically. Its own package, not part of `Navigation`, because it needs no JS.
- **Barcodes** — machine-readable graphics generated from a value, rendered as SVG. "Barcode" is the umbrella: `AtomBarcode` for 1D/linear (Code128, EAN-13, Code39…) and `AtomQrCode` for 2D/matrix (QR). **Generation only, using our own C# encoder** — a third-party lib (QRCoder/ZXing) would break the 0-dep rule. Note: *reading/scanning* a code needs a camera + JS → a separate Tier C concern, not part of this library. *(Implemented: Code39, Code128, EAN-13, UPC-A, ITF, Codabar (1D) + QR byte-mode v1–40 (2D), verified by ZXing round-trip / reference tests.)*
- **Equipment** — semi-realistic status indicators for physical objects/equipment (an SCADA/HMI-style status board), as opposed to `Progress`'s task-percentage meters or `ActivityIndicators`'s working-loaders. Shipped: `AtomStoplight` (host-driven, one-way `StoplightState`); `AtomLightBulb` and `AtomLightSwitch` (click/keyboard-interactive with `@bind-IsOn`, since a light is something you actually operate); `AtomFan` (click/keyboard-cycles `FanSpeed` via `@bind-Speed`, separate one-way `Direction`, Desk or Ceiling housing). Planned: a door/window open-closed monitor, an elevator direction indicator.

## Tier B — feasible, minor/optional JS

| Library | Components (`Atom*`) | Note |
|---|---|---|
| `BlazorAtoms.Inputs` *(in progress)* | **The standard fields, all shipped** — AtomTextField, AtomTextArea, AtomNumberField&lt;TValue&gt;, AtomCheckbox, AtomSwitch, AtomRadioGroup&lt;TValue&gt;, AtomSelect&lt;TValue&gt;: seven native controls (`input`/`textarea`/`select`) on one **AtomInputBase&lt;TValue&gt;** — shared `@bind-Value` + `EditContext` glue, label/help/`Visible`, and three styling axes (`Variant` Outline/Filled/Underline, `Size` Small/Medium/Large, `Effect` None/FocusGlow/FocusRaise/FocusUnderline/ShakeOnError) emitted as `data-*` over a `--field-*` custom-property surface, so a look is one CSS block and no C# change. `ReadOnly` renders a real native `readonly` where the platform has one (text/textarea/number) and folds into `disabled` where it doesn't (checkbox/switch/radio/select). Checkbox/switch/radio paint their own box/track/mark over a transparent-but-focusable native input, which is also how the JS-only `indeterminate` flag is faked. Also shipped: AtomRangeInput, AtomCrtInput, AtomCrtDisplay. Still to build: nothing on the core-forms list | core forms; JS-free throughout |
| `BlazorAtoms.Buttons` *(shipped)* | **AtomButton** — native `<button>`, or a real `<a>` when `Href` is set (no `role="button"`: it navigates). Color `Variant` ×  Solid/Soft/Outline/Ghost/Link `Appearance` × `Size` × Rounded/Square/Pill/Circle `Shape`, icon slots, `Loading` (spinner swaps in, content keeps its space), `Pressed` as `bool?` so a plain button never claims toggle semantics. **ButtonEffect**: Press3d, Bevel, GradientBorder, Rainbow, Fizzy, Storm — pure CSS — plus ClickRipple, the one member with any C# (origin from `MouseEventArgs.Offset*`, per-click `@key` to restart the keyframe; still no JS). **AtomIconButton** / **AtomToggleButton** *render* an AtomButton rather than re-implementing it, so `AtomButton.razor.css` is the family's single stylesheet — the opposite call from `BlazorAtoms.Inputs`, whose 7 fields each own their CSS, because seven long effect blocks are not worth duplicating five ways. **AtomButtonGroup** — seams the children (flattened inner radii + negative margin so two borders read as one) and cascades the four axes via `ButtonGroupContext`; layout only, no selected value. **AtomSplitButton** — action half is an AtomButton, menu half a native `<details>`/`<summary>` (free open state + keyboard, but no collision flip and no click-outside close — both need JS; composes `Overlays.AtomDropdown` once that exists). All five share `ButtonFamilyBase` and one `--btn-*` token prefix | JS-free |
| `BlazorAtoms.Cards` *(shipped)* | **Two families, sharing no base.** *(1) The structural card, all shipped:* **AtomCard** — a themed surface holding an edge-to-edge `Media` slot (`MediaPosition` Top/Bottom/Start/End; the inline values make it a horizontal media object, and media renders before or after the sections in DOM order so the CSS needs no `order`) plus **AtomCardHeader** (`Title` as a real `h1`–`h6` at `HeadingLevel`, `Subtitle`, `Avatar`, end-aligned `Actions`), **AtomCardBody** (`Scrollable` also sets `tabindex="0"` — a scroll box that can't be focused is unreachable without a mouse), **AtomCardFooter** (`Align`). Sections arrive either as `Header`/`Body`/`Footer` slots or nested in `ChildContent`. Root element follows the semantics — `<div>`, a real `<a>` when `Href` is set (no `role="button"`: it navigates), a `<button>` when only `OnClick` is. `Variant` (Elevated/Outlined/Filled/Flat) × `Elevation` × `CardEffect` (HoverLift/HoverGlow/HoverBorder/PressSink, pure CSS) over a `--card-*` surface. Padding and dividers cascade through **CardContext**; every inherited param on **AtomCardSectionBase** is nullable, so precedence is a plain `??` chain with no "was it set?" detection (contrast `ButtonFamilyBase`, whose non-nullable enum axes force it to inspect the `ParameterView`). *(2) The hover-reveal family:* each shows a themed face (title/subtitle/background image/dot indicator) with a staggered entrance on mount, then uncovers a body panel on hover; they differ in *how*. **AtomCardReveal** *(shipped)* — overlay slides away along a `Direction` (Left/Right/Up/Down) leaving an image sliver; `RevealSize` is measured along the active axis, hence "size" not "width". **AtomCardFlip** *(shipped)* — card rotates 180° around `FlipAxis`, body is the back face. **AtomCardExpand** *(shipped)* — card grows to `ExpandedHeight` and the body slides up from the bottom (height transition, not `scale()`, so image/text don't distort). **AtomCardCurl** *(shipped)* — a `Corner` peels back; a CSS corner *fold*, not a photorealistic curl (CSS can't warp a plane — that needs an SVG filter or WebGL). **AtomCardSplit** *(shipped)* — the face splits along a `SplitAxis` and both halves swing open, hinged on their outer edges; each half carries the whole image pinned to its own outer edge so the closed card reads as one unbroken picture, and the halves have no back faces (they stop being drawn past 90deg) which keeps the revealed body a SINGLE element — text is never split at the seam nor duplicated in the DOM. Optional `ShowSeamCircle` reproduces the source design's circle straddling the seam. All five share 13 params via **AtomCardBase**, emitted as family-wide `--atom-card-*` custom properties — including `BorderWidth`/`BorderColor`, which decouple the frame from `AccentColor` (`BorderWidth="0"` removes it; previously the frame was a hardcoded 8px and `AccentColor="transparent"` both left its space as a gap and made the card face see-through). Separate components rather than one `CardEffect` enum because each needs a param the others can't use — the opposite call from `AtomTransition`, whose 20 effects share one enum precisely because they add nothing per-effect. 3D *tilt* deliberately lives in `BlazorAtoms.Transitions` as `HoverEffect.Tilt` (reveals nothing → needs no card structure; composes around any card). The two families are not unified because neither base's params mean anything on the other — `AtomCardBase`'s background image and dot count serve a reveal `AtomCard` doesn't have, and `AtomCard`'s section cascade has no place on a fixed two-panel face | JS-free |
| `BlazorAtoms.Tabs` *(shipped)* | **AtomTabs** — owns the selected value and renders the `role="tablist"` strip plus the panel region; **AtomTab** and **AtomTabPanel** pair by a string `Value`. Two named slots (`TabList`/`Panels`) rather than one flat child list, because Blazor renders children in source order and interleaved tabs/panels would need nodes hoisted across the render tree. Full ARIA tabs pattern: roles, `aria-selected`, `aria-controls`↔`aria-labelledby` ids (Guid prefix generated once in a field so it survives prerender→interactive; the caller's `Value` is slugged, since a space in an id breaks the reference), roving `tabindex`, Arrow/Home/End skipping disabled tabs, `Automatic` vs `Manual` activation. **Selection is derived, never assigned** — an unset or unmatched `Value` shows the first enabled tab *without* raising `ValueChanged`, so `@bind-Value` performs no surprise write during initialization. **TabPanelRender** Active/Always/Lazy decides whether inactive panels keep browser-owned state (uncommitted input, scroll, playback). Line/Enclosed/Pill/Bar `Variant` × `Size` × `Orientation` (also sets `aria-orientation`, which picks the arrow keys) × `Align` (`Stretch` grows the tabs, so it lives on the tab, not the strip's `justify-content`) over a `--tabs-*` surface. Unlike `ButtonGroupContext`/`CardContext` the cascade carries the *component*, not a DTO, because children must register for the arrow order and be focused by the parent — and being `IsFixed` over an unchanging reference, selection changes propagate by explicit `StateHasChanged`, not by subscription. Uses no `::deep`: tab/panel rules key off an ancestor `.atom-tabs[data-*]`, since the scope rewriter only appends the scope id to the final selector One self-imported module (`atom-tabs.js`, ~30 lines) that exists solely to `preventDefault` the browser's scroll for the keys the component consumes — Blazor's `:preventDefault` is render-time, so it can't be applied per key without also swallowing Tab. Its key list mirrors the C# switch per axis (read from `aria-orientation` at event time) and skips modified keypresses and text-entry targets; a failed load degrades to arrows-that-also-scroll, so it's a convenience, not a dependency. Focus movement uses `ElementReference.FocusAsync()` (framework interop), guarded the same three ways as every other interop call site in the repo — though bUnit bypasses its mock for focus, so that guard is inspection-only. Keyboard needs an interactive render mode for the ordinary reason (no handlers run under static SSR); clicking works everywhere |
| `BlazorAtoms.Panels` | AtomAccordion, AtomCollapse, AtomPanel, AtomSplitter | accordion/collapse via native `<details>`; splitter uses pointer events — no JS |
| `BlazorAtoms.RadialMenus` *(shipped)* | **AtomRadialMenu** — a center button whose items radiate on a consumer-defined arc, **0° up and clockwise**, where any item is a leaf action or a branch opening a ring of its own to any depth. **Item count is never capped:** the radius is *solved* from collision geometry (`R ≥ (ItemSize+ItemGap)/(2·sin(sep/2))`, plus a hub-clearance constraint), so adding items grows the ring instead of overlapping it — and the separation solved against is the *measured minimum circular gap*, not the nominal step, because a 350° arc of three items has a 175° step but 10° neighbours across the wrap. When space is bounded, `Overflow` decides what gives: `GrowRadius`/`Rings` (concentric, staggered half a step)/`Shrink` (to `MinItemSize`)/`Paginate` (with stepper slots)/`Spin` (a rotating window, driven by an ordinary Blazor `@onwheel`). `Distribution` is `Auto` → `Cyclic` on a closed arc so four items land on 0/90/180/270, `Endpoints` on a partial arc so the first and last sit on the bounds; `Padded` and `FixedStep` are explicit. **Nesting needs no angles from the consumer:** `ExpandMode` Cascade (children radiate from the branch, arc centred on the direction it already points)/Concentric (next ring out, confined to the parent's slice)/Drill (children replace the ring, the center goes back and names the level — same footprint at any depth), with per-item `StartAngle`/`EndAngle` as the override. **`MaxVisibleDepth` bounds a deep tree by re-rooting** rather than by clipping — slice containment divides each level's arc by the branching factor, so `R ≥ (ItemSize+ItemGap)/(2·sin(arc/2))` roughly doubles the radius per level (64, 126, 183, **329**, 604 on a 180° arc with 3-way branching) and no `ArcMode` fixes it: `InheritSweep` buys a linear radius by abandoning containment, and `MinItemSize` floors any attempt to shrink items to match. Capping the visible levels caps how far the arc can narrow; the ancestor that falls out of the window becomes the center button, which names it and goes back — `Drill` is the same idea with a window of 1. True depth still reaches `data-depth`/`data-path`; only size, radius and overflow state are measured from the visible frame. Shapes are **real SVG polygons** (circle→octagon, `Polygon(N)`, or a custom path) in a fixed 100×100 box, not `clip-path`, so stroke and focus ring both survive; sizing solves for the shape's **inscribed** circle (`S = √(w²+h²)/cos(180/n)` — 1.41× a label's diagonal in a circle, 1.63× in a hexagon, 2.83× in a triangle) via `Fixed`/`FromFont`/`Measure`, and `LabelPlacement=Outside` sidesteps it entirely. All geometry lives in `RadialLayout` + `RadialShapeGeometry`, pure functions with no Blazor/DOM/JS types in reach, so it is unit-tested without a renderer against hand-computed numbers. Recursion is flattened into one ring list rather than a child component, so nothing internal leaks onto the consumer's surface. `CollapseAllAsync()` via `@ref` is the one imperative API — the only way to reset expansion state under `Trigger=Always`, which pins the ring open and so never runs the close path that clears it (it matters when swapping a data set, because open paths are positional). Roving-tabindex keyboard nav (arrows around/between rings, Esc unwinds), `Debug` overlay drawing arc bounds/ticks/angle-and-path tags plus advisories from two sources — the per-ring solve, and a cross-ring check that catches the overlap a pure per-ring solve structurally cannot see (a deep Cascade ring folding back over the center button, or two sibling subtrees each solved in ignorance of the other), optional spokes, `prefers-reduced-motion` honoured | Tier B, but the module is **always-on** rather than per-feature (one code path, no JS-free fallback branch to maintain) — see DEVELOPMENT.md. JS supplies only what the browser alone knows: container box for `RadiusMode=FitContainer`, real text widths for `SizeMode=Measure`, and outside-pointer-down for `CloseOnOutsideClick`. Default `SizeMode=Fixed` means full geometry prerenders server-side with no reflow |
| `BlazorAtoms.DynamicFormWizard` *(shipped)* | **AtomFew*-style atom, but a headless engine rather than a leaf component** — a POCO decorated with `[FormStep]`/`[FormOrder]`/`[DependsOn]`/`[FormPathEnd]`/`ValidationAttribute`s drives **`DynamicWizard<TModel>`**, which renders the whole multi-step flow itself: branching + free rejoining (a step with no `[DependsOn]` shows to every path that reaches it — no merge construct), an authoritative `[FormPathEnd]` override for a branch's true end (safer than gating every later field individually), dynamically-recomputed step label/position (never a raw step number or a static total — two branches through the same model can report different true lengths), and partial per-step validation (`Validator.TryValidateValue` for leaves, `TryValidateObject` for auto-expanded complex-typed properties). A four-tier field-render dispatch — consumer `FieldRenderers` type registry → built-in scalar (bool/enum/string/DateTime/number/file) → auto-expand a complex type's own properties into a field group → read-only `ToString()` fallback — means an unhandled type never silently disappears. File uploads (`IReadOnlyList<WizardFileAttachment>`) copy bytes into memory immediately on selection, never a raw `IBrowserFile` handle; `[FormSelect]`/`[FormDynamicSelect]` (the latter via a DI-registered `IWizardLookupService`, resolved lazily so it's never a forced dependency) render dropdowns; `[FormLayout]` lays fields into a bare-CSS grid (`--wizard-column-span`, no framework classes) instead of stacking one-per-row. `FieldTemplate` (whole-form override) and `FieldRenderers` (single-type override, e.g. a `Money` struct) are the two extensibility seams — see `EXTENSIBILITY.md`. Deliberately **not** a 0-dependency leaf like every sibling package: it's an orchestrator whose entire job is generating UI from metadata, and a future render-adapter package (not yet built) is the only thing expected to ever depend on `BlazorAtoms.Inputs` for an Atom*-styled look. Interactive render modes only (reflection + `EditContext` + file upload all require one). See `DESIGN-DISCUSSION.md` for the full decision log, `FLOW.md` for diagrams | interactive-only; JS-free |
| `BlazorAtoms.Tooltips` *(shipped)* | AtomTooltip — pure-CSS bubble (rect/pill/ellipse/thought/burst/folded); CSS placement + `:hover`/`:focus-within` · AtomShapedTooltip — outline drawn as inline SVG so border works on every shape (adds cloud) · AtomPaintedTooltip — SVG that also paints (gradient fill, SVG stroke, optional shadow). Shared `Placement`; per-component `Shape` enums. JS-free except the opt-in `Cursor` follow mode (self-loaded module). |
| `BlazorAtoms.Highlights` *(implemented)* | AtomHighlight — zero-JS highlighter for plain-text child content; renders `<mark>` during Blazor render, works in every render mode · AtomHighlightDeep — zero-JS highlighter for rich HTML content (mixed markup: headings, lists, tables, links) supplied as an HTML string; wraps matches in `<mark>` during Blazor's render pass, safe across re-renders. Both share `HighlightStyle` (`Mark`/`Underline`/`Outline`) and CSS variables. · AtomHighlighter — highlights keyword matches in the live DOM via a self-imported JS module scoped to its container; works through arbitrarily nested child components since it operates on real rendered output, not the render tree. |
| `BlazorAtoms.Alerts` | AtomAlert, AtomToast, AtomBanner, AtomCallout | toast timing via C# timer |
| `BlazorAtoms.Icons` | AtomIcon (+ optional curated SVG set) | renderer is 0-dep; a bundled icon set is larger |
| `BlazorAtoms.Typography` *(in progress)* | AtomTextCycle *(shipped)* — zero-JS vertical flip-cascade word rotator, cycles `Words` in an infinite loop; per-instance-generated `@keyframes` sized to word count, duplicate-first-word row for a seamless wrap. AtomTextScramble *(shipped)* — zero-JS one-shot per-character entrance animation for a single `Word` (7 effects), static scoped CSS (keyframes don't vary by instance), auto-replays on `Word` change, optional `Replay()` method for on-demand repeat. AtomTextLava *(shipped)* — zero-JS single word rising out of an animated molten-lava-gradient background; `Loop` (default on) bubbles up/down forever via `animation-direction:alternate` reusing one keyframe block, off rises once and holds. AtomTextSparkle *(shipped)* — zero-JS hover effect (layered 3D text-shadow, colorized glare sweep, scattered SVG sparkles), pure CSS `:hover`/`:active` trigger with no C# state at all; sparkle positions are a deterministic function of index (not `Random`) to avoid a hydration mismatch jump. Still to build: AtomHeading, AtomText (remaining hover/loop text effects), AtomCode, AtomKbd, AtomTruncate | JS-free |
| `BlazorAtoms.Clocks` *(shipped)* | AtomClock — live single-zone clock (server/browser/UTC or explicit zone), opt-out ticking, semantic `<time>` · AtomAnalogClock — same sources on a scalable SVG dial (hands, minute ticks, numerals) · AtomClockPair — two clocks (e.g. server + local) side-by-side or stacked · AtomClockStrip — N-zone world-clock row/grid/list (digital or analog, viewer highlight, relative offset, sort, select) · AtomTimeZoneMap — inline-SVG world map: continents + 24 UTC±N bands + day/night terminator + sun marker + accurate city pins · AtomTimeZonePicker — searchable combobox over every system zone (filter, region groups, per-zone offset, "use my zone" detect), `@bind-Value` on the IANA id | browser-tz detect via self-loaded JS module (opt-in); ticking via C# `PeriodicTimer`; city times/zone list via `TimeZoneInfo`; no map service/CDN/raster; else JS-free |

**What each does:**
- **Inputs** — the core form fields, two-way bound to your data. The seven standard ones (text, textarea, number, checkbox, switch, radio group, select) are shipped and share one base: one `@bind-Value` contract, one `EditContext`/`DataAnnotations`-aware error state, and `Variant`/`Size`/`Effect` axes over `--field-*` custom properties, so the whole family themes together. Each wraps a native control, so keyboard behavior, form submission, and mobile pickers come from the platform. `AtomRangeInput` is a labeled slider on the same `EditContext` contract (its own, older copy of the glue — see DEVELOPMENT.md).
- **Buttons** — clickable actions: standard, icon-only, toggle (`aria-pressed`), grouped (one seamed control that sets the axes once for the set), and split (primary action + a native `<details>` menu). Each wraps a native element, so keyboard, form submission, and link affordances are the platform's. `Effect` adds opt-in CSS decoration — 3D press, bevel, gradient border, rainbow, fizzy, storm, click ripple — all off by default and all suppressed under `prefers-reduced-motion`.
- **Cards** — two families. `AtomCard` with `AtomCardHeader`/`AtomCardBody`/`AtomCardFooter` is the
  plain box you put a summary or preview in: a themed surface with a media slot, elevation, optional
  whole-card link or click, and sections that inherit padding and dividers from the card. The
  hover-reveal cards (`AtomCardReveal`, `AtomCardFlip`, `AtomCardExpand`, `AtomCardCurl`,
  `AtomCardSplit`) each show a themed face that plays a staggered entrance on mount, then uncovers a
  body panel on hover — a slide, a flip, a grow, a corner fold, a split. All pure CSS, no C# trigger
  state.
- **Tabs** — switch between panels of content in the same space via a tab strip. `AtomTabs` plus
  `AtomTab`/`AtomTabPanel` paired by a string key, with the whole ARIA keyboard pattern (arrows, Home/End,
  roving tabindex) and a choice of whether inactive panels stay in the DOM keeping their state.
- **Panels** — expand/collapse content regions: accordion, collapsible section, and resizable splitter.
- **Tooltips** — small hover/focus hint bubbles shown beside an element.
- **Alerts** — messages to the user: inline alert/callout, a page banner, and toast (an auto-dismissing popup notification).
- **Icons** — an `AtomIcon` renderer for SVG glyphs, optionally paired with a bundled icon set.
- **Typography** — text primitives. `AtomTextCycle` (shipped) cycles a list of words/phrases in an
  infinite vertical flip-cascade loop, zero JS. `AtomTextScramble` (shipped) plays a one-shot
  per-character entrance animation for a single word (7 effects) — not a cycling component,
  auto-replays on word change, `Replay()` for on-demand repeat. `AtomTextLava` (shipped) rises a
  single word out of an animated molten-lava background, looping (bubbling) by default or
  one-shot via `Loop="false"`. `AtomTextSparkle` (shipped) is a hover effect (3D text-shadow,
  glare sweep, scattered sparkles) driven by pure CSS `:hover`/`:active` — no C# trigger state at
  all. Still to build: headings, body text, inline code, keyboard keys, truncation.
- **Clocks** — live time displays: `AtomClock` ticks a single zone (server, UTC, browser-local, or an explicit `TimeZoneInfo`); `AtomAnalogClock` shows the same on a scalable SVG dial; `AtomClockPair` shows two (e.g. server + local) side-by-side or stacked; `AtomClockStrip` shows N zones as a world-clock row/grid/list; `AtomTimeZoneMap` is a whole-earth timezone map (inline SVG continents + UTC±N bands + day/night terminator + accurate city pins); `AtomTimeZonePicker` is a searchable dropdown over every system timezone (filter, region groups, per-zone offset, auto-detect), two-way bound on the IANA id. Browser zone is auto-detected via a tiny self-loaded JS module.

## Tier C — heavier (needs JS interop or a dependency). Decide later; may dent the 0-dep goal.

| Library | Reason it's heavier |
|---|---|
| `BlazorAtoms.Canvas` *(shipped)* | AtomCanvas — declarative, serializable shape model (line/rect/circle/freehand path/text/image) with Static/Draw/Select/Pan modes, selection + zoom/pan view, + a batched imperative `Canvas2DContext` escape hatch · AtomSignaturePad — signature pad (`@bind-Value` PNG data-URL, Clear/Undo, PNG/SVG export) · AtomCanvasStudio — batteries-included, extensible workbench (toolbar, shape/stamp insert with click-to-place, pen/fill/bg, undo/redo, zoom/pan, layers panel, save/load JSON, PNG/SVG export) with a public slot + cascading-`AtomCanvasStudioContext` extension API (the family's first `CascadingValue`). The family's first intentional `<canvas>` — justified by the Graphics-policy hard-perf carve-out (60 fps freehand ink + `toDataURL` raster export SVG can't do). Ships and self-imports its own `atom-canvas.js` (no `<script>`/DI); gesture stays in JS so it's smooth on Server. |
| `BlazorAtoms.Screensavers` *(shipped)* | AtomScreensaverRain — Matrix-style digital rain on a `<canvas>`, animated by a self-imported JS module; `TextColor`/`BackgroundColor`/`FontFamily`/`FontSize`/`Speed`/`Width`/`Height` plus `Glow` and `Scanlines` as `data-*`, `Disabled` to drop the canvas entirely, and `StartAsync`/`StopAsync` to control the loop. Tier C for the same two reasons as `Canvas`: raster surface **and** a per-frame animation loop that has to live in JS to stay smooth on Server. Naming now follows the conventions throughout: the component was `ScreensaverRain` (the repo's only public type without the `Atom` prefix), its module was `MatrixRain.js` rather than the policy's `atom-<area>.js`, its CSS classes were `matrix-rain*`, and its custom properties were `--mr-*`; all four are renamed. Worth knowing that the `--screensaver-rain-*` properties are a three-way contract — C# writes them into the root `style`, the stylesheet declares the fallbacks, and the JS module reads them back with `getComputedStyle` every frame so live control changes apply without a restart — so renaming one prefix means renaming it in three places at once. |
| `BlazorAtoms.Overlays` (Modal, Drawer, Popover, Dropdown, Menu) | positioning + focus-trap + scroll-lock usually want JS. **Name collision to settle first:** `AtomDrawer` already ships in `BlazorAtoms.Layout`, so a second one here would give any app referencing both an ambiguous tag — either drop Drawer from this list or move the existing one. Also the unblocker for `AtomSplitButton`, whose `<details>` menu has no collision flip and no click-outside close; `AtomDropdown` in turn wants `Behaviors`' planned ClickOutside + FocusTrap, so the order is Behaviors → Overlays → retrofit SplitButton. |
| `BlazorAtoms.Pickers` (Date / Time / Color) | popup/calendar logic; color picker is closer to JS-free |
| `BlazorAtoms.Tables` / `DataGrid` | a simple table is JS-free; sorting/virtualization is heavy |
| `BlazorAtoms.Navigation` *(shipped)* | AtomScrollTo — scroll-to-top/bottom or scroll-to-anchor button; default SVG chevron or custom-icon slot, tooltip/aria, page-or-container scope, optional auto-hide-until-scrolled (passive + rAF-coalesced watcher); self-imports `atom-navigation.js` for smooth scroll + visibility. *Planned:* Pagination, Stepper. (Breadcrumbs shipped separately as `BlazorAtoms.Breadcrumbs` — Tier A, since it needs no JS.) |
| `BlazorAtoms.Behaviors` *(shipped)* | **AtomBrowserSupport** — cached runtime CSS-feature-support check (`CSS.supports()` via a tiny self-imported JS module) · **TransitionState** — reusable enter/exit animation state machine, the engine behind `BlazorAtoms.Transitions`'s `AtomTransition`, usable directly by future components (carousel, text animation, image effects) without wrapper markup. *Planned:* ClickOutside, FocusTrap, Clipboard, Portal. |

**What each does:**
- **Canvas** — native `<canvas>` drawing behind a clean C# API: a declarative shape model, freehand ink (signature capture), and drag-to-move, plus a batched raw-2D-context escape hatch. Tops out at `AtomCanvasStudio`, a full drop-in workbench (toolbar/stamps/layers/undo-redo/zoom/save-load) that is extensible via slots + a cascading context. The family's first raster surface; owns its own tiny JS module so consumers write no interop.
- **Screensavers** — full-bleed ambient animations for an idle screen or a decorative backdrop.
  `AtomScreensaverRain` is the Matrix digital-rain effect, themeable (color, font, speed, glow,
  scanlines) and startable/stoppable from C#. Drawn on `<canvas>` with the frame loop in JS, so it
  sits in Tier C beside `Canvas` rather than with the CSS/SVG effects in `Transitions`.
- **Overlays** — things that float above the page: modal dialog, side drawer, popover, dropdown, menu.
- **Pickers** — specialized value selectors for date, time, and color.
- **Tables** — tabular data display, from a simple static table up to a sortable/virtualized data grid.
- **Navigation** — moving around an app. Shipped: `AtomScrollTo` (scroll-to-top/bottom/anchor button, page or container scope, auto-hide-until-scrolled). Planned: breadcrumbs, pagination, stepper.
- **Behaviors** — headless helpers with no visuals of their own. Shipped: `AtomBrowserSupport` (runtime CSS-feature detection) and `TransitionState` (the enter/exit engine behind `AtomTransition`). Planned: click-outside, focus-trap, clipboard copy, portal.

---

## Suggested first wave (after ActivityIndicators)

`Progress` → `Skeletons` → `Badges` → `Charts`

All Tier A: they prove the packaging template, ship quickly, and keep the 0-dependency guarantee.

**Status: all four shipped.** The only Tier A work left is `Layout`'s six planned primitives
(`AtomStack`, `AtomGrid`, `AtomDivider`, `AtomSpacer`, `AtomCenter`, `AtomAspectRatio`) beyond the
`AtomDrawer` it already has. Tier B still owes `Panels`, `Alerts`, `Icons` and five `Typography`
components; Tier C owes `Overlays`, `Pickers`, `Tables`, two `Navigation` components and four `Behaviors`.

> Status: brainstorm / not committed to. Names and groupings are provisional — refine before scaffolding.
