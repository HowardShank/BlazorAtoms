@* The deepest node in the nesting demo — AtomHighlighter never sees or modifies this component;
   it only scans the real DOM its container ends up containing after Blazor renders everything. *@
<p>Build interactive web UIs with Blazor and modern C# — no JavaScript required for most of it.</p>
